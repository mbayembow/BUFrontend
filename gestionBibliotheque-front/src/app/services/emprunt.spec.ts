import { TestBed } from '@angular/core/testing';

describe('Emprunt', () => {

  beforeEach(() => {
    TestBed.configureTestingModule({});

  });

  it('should be created', () => {
    // Ici on ne peut pas tester une interface, donc on met juste un expect basique
    expect(true).toBeTruthy();
  });
});

